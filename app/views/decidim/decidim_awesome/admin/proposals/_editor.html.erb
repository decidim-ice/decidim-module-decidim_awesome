<% if Decidim::DecidimAwesome.enabled?(:proposal_custom_fields) && editor = admin_editor_for_proposal_body(form) %>
  <%= editor %>
<% else %>
  <%= form.translated :editor, :body, hashtaggable: true, aria: { label: :body } %>
<% end %>

<% if Decidim::DecidimAwesome.enabled?(:proposal_private_custom_fields) %>
  <%= render_proposal_custom_fields_override(awesome_proposal_custom_fields_for(:private_body), form, :private_body) %>
<% end %>
