<div class="form__wrapper">
  <div class="item__edit-form">
    <% form_url = landing_menu_item_path(id: params[:id], content_block_id: params[:content_block_id]) %>
    <%= decidim_form_for(@form,
                         url: form_url,
                         method: :patch,
                         remote: true,
                         html: { id: "landing-menu-item-form", class: "form-defaults" }) do |f| %>
      <h1>
        <%= t(".title") %>
        <%= f.submit t(".save"), class: "button button__sm button__secondary" %>
      </h1>
      <%= render partial: "form", locals: { form: f } %>
    <% end %>
  </div>
</div>
